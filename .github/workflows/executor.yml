name: 🛠️ PHOENIX EXECUTOR

on:
  workflow_dispatch:
    inputs:
      task_payload:
        required: true

permissions:
  contents: write
  actions: write

jobs:
  execute:
    runs-on: ubuntu-latest
    steps:
      - name: 1.1 - Decode Thought
        run: |
          echo "${{ github.event.inputs.task_payload }}" | base64 -d > task.py

      - name: 2.1 - Execute & Feedback
        env:
          GH_TOKEN: ${{ secrets.COMMANDER_PAT }}
          # توكن احتياطي من النظام
          SYS_TOKEN: ${{ github.token }}
          OWNER: ${{ github.repository_owner }}
          REPO: "avatar-template"
        run: |
          # ضمان وجود توكن فعال
          [ -z "$GH_TOKEN" ] && export GH_TOKEN="$SYS_TOKEN"

          python3 task.py > output.log 2> error.log || echo "FAILED" > status.txt
          
          if [ -f status.txt ]; then
            ERR_B64=$(cat error.log | tail -c 1000 | base64 -w 0)
            SHA=$(gh api /repos/$OWNER/$REPO/contents/error_log.txt --jq '.sha' || echo "null")
            if [ "$SHA" == "null" ]; then
              gh api --method PUT /repos/$OWNER/$REPO/contents/error_log.txt -f message="⚠️ Error" -f content="$ERR_B64"
            else
              gh api --method PUT /repos/$OWNER/$REPO/contents/error_log.txt -f message="⚠️ Update" -f content="$ERR_B64" -f sha="$SHA"
            fi
            exit 1
          else
            SHA=$(gh api /repos/$OWNER/$REPO/contents/error_log.txt --jq '.sha' || echo "null")
            [ "$SHA" != "null" ] && gh api --method DELETE /repos/$OWNER/$REPO/contents/error_log.txt -f message="✅ Clean" -f sha="$SHA" || true
          fi