name: 🛠️ PHOENIX EXECUTOR

on:
  workflow_dispatch:
    inputs:
      task_payload:
        required: true
      run_id:
        required: false

jobs:
  execute:
    runs-on: ubuntu-latest
    steps:
      - name: 1.1 - Setup
        run: |
          echo "${{ github.event.inputs.task_payload }}" > task.py
          echo "--- CODE RECEIVED ---"
          cat task.py

      - name: 2.1 - Run & Debug
        run: |
          echo "--- EXECUTION START ---"
          # تشغيل الكود مع صيد الأخطاء فوراً
          python3 -u task.py 2>&1 | tee execution_output.log
          
          # فحص إذا كان هناك فشل
          if [ ${PIPESTATUS[0]} -ne 0 ]; then
            echo "❌ Execution Failed"
            exit 1
          fi
          echo "--- EXECUTION SUCCESS ---"

      - name: 3.1 - Summary
        if: always()
        run: |
          echo "### Task ID: ${{ github.event.inputs.run_id }}" >> $GITHUB_STEP_SUMMARY
          echo '```python' >> $GITHUB_STEP_SUMMARY
          cat execution_output.log >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY