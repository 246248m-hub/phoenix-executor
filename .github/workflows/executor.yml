name: 🛠️ PHOENIX EXECUTOR

on:
  workflow_dispatch:
    inputs:
      task_payload:
        required: true
      run_id:
        required: false

jobs:
  execute:
    runs-on: ubuntu-latest
    steps:
      - name: 1.1 - Prepare Task
        run: |
          echo "${{ github.event.inputs.task_payload }}" > task.py
          if [ ! -s task.py ]; then echo "❌ Empty Payload"; exit 1; fi

      - name: 2.1 - Run & Debug
        run: |
          echo "--- EXECUTION START ---"
          # تشغيل الكود وتخزين المخرجات والخطأ
          python3 -u task.py 2>&1 | tee execution_output.log
          if [ ${PIPESTATUS[0]} -ne 0 ]; then
            echo "❌ Execution Failed"
            exit 1
          fi
          echo "--- SUCCESS ---"

      - name: 3.1 - Generate Summary
        if: always()
        run: |
          echo "### 🚀 Evolution Summary" >> $GITHUB_STEP_SUMMARY
          echo "#### Output Log:" >> $GITHUB_STEP_SUMMARY
          echo '```python' >> $GITHUB_STEP_SUMMARY
          cat execution_output.log >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY